BFS 
1.特点：按照从距离开始状态由近及远的顺序进行搜索
2.应用场合：适用于求最短路径、最少操作之类的问题
3.例子
迷宫的最短路径

4. 同DFS比较
BFS & DFS都能遍历到所有状态，一般来说，递归函数更容易编写，代码会更精简，因此需要遍历所有状态时一般采用DFS。
而求最短路径类型问题，使用DFS会反复遍历相同的状态，因此宜采用BFS。
BFS会将状态逐个加入队列，需要的空间同状态数成正比，而DFS与递归深度成正比，一般来说DFS更节省空间。
IDDFS，综合BFS & DFS的优点。
